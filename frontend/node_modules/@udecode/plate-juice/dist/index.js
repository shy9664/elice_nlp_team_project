'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var plateCore = require('@udecode/plate-core');
var juice = require('juice');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var juice__default = /*#__PURE__*/_interopDefaultLegacy(juice);

const KEY_JUICE = 'juice';
const createJuicePlugin = plateCore.createPluginFactory({
  key: KEY_JUICE,
  inject: {
    pluginsByKey: {
      [plateCore.KEY_DESERIALIZE_HTML]: {
        editor: {
          insertData: {
            transformData: data => {
              // juice ignores the first class when there is <!-- just after <style>, so we remove it
              let newData = data.replace(/<style>(\s*?)<!--/g, '<style>');
              newData = juice__default['default'](newData);
              return newData;
            }
          }
        }
      }
    }
  }
});

exports.KEY_JUICE = KEY_JUICE;
exports.createJuicePlugin = createJuicePlugin;
//# sourceMappingURL=index.js.map
